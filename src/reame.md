# rust grpc microservice
    rust grpc 使用 tonic 框架实现，更多用法参考：https://github.com/daheige/rs-grpc

# start service
```shell
cd ../
CONFIG_DIR=$(pwd)
cargo run --bin hephfx-micro-svc
```

# client call
```shell
cargo run --bin hello-client
```
